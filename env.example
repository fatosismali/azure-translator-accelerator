# Azure Translator Configuration
# Get your key and endpoint from Azure Portal: https://portal.azure.com

# Required: Azure Translator API Key (for local dev)
AZURE_TRANSLATOR_KEY=your-translator-key-here

# Required: Azure Region where your Translator resource is deployed
AZURE_TRANSLATOR_REGION=westeurope

# Required: Azure Translator Endpoint
AZURE_TRANSLATOR_ENDPOINT=https://api.cognitive.microsofttranslator.com

# Optional: Custom endpoint if using private endpoint or different region
# AZURE_TRANSLATOR_CUSTOM_ENDPOINT=https://your-custom-endpoint.com

# Azure AI Foundry (for LLM translation with GPT-4o/GPT-4o-mini)
# Required for 2025-05-01-preview API LLM features
# Get these from Azure Portal after deploying AI Foundry resource
AZURE_AI_FOUNDRY_ENDPOINT=
AZURE_AI_FOUNDRY_KEY=
GPT4O_MINI_DEPLOYMENT_NAME=gpt-4o-mini

# Azure Storage (for batch translation)
# For local development, use connection string
# For cloud deployment, use managed identity (leave connection string empty)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_NAME=

# Key Vault (for cloud deployment with Managed Identity)
# Leave empty for local development
AZURE_KEY_VAULT_URL=
AZURE_STORAGE_CONTAINER_NAME=translations

# Application Insights (for telemetry and monitoring)
# Leave empty to disable telemetry in local dev
APPLICATIONINSIGHTS_CONNECTION_STRING=
APPINSIGHTS_INSTRUMENTATIONKEY=

# Application Configuration
APP_NAME=translator-accelerator
APP_VERSION=1.0.0
LOG_LEVEL=INFO
ENVIRONMENT=development

# API Configuration
API_V1_PREFIX=/api/v1
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Batch Translation Queue Mode
# Enable queue-based async processing (recommended for production)
# Set to false for synchronous processing (testing/debugging only)
ENABLE_BATCH_QUEUE=true

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# Feature Flags
ENABLE_TELEMETRY=false
ENABLE_CACHING=false
ENABLE_TRANSLITERATION=true
ENABLE_DICTIONARY=true

# Frontend Configuration (for build time)
VITE_API_BASE_URL=http://localhost:8000
VITE_APPINSIGHTS_CONNECTION_STRING=

# Development Tools
DEBUG=false
RELOAD=true

# Azure Authentication (for local development with Azure CLI)
# These are automatically set by Azure CLI, no need to configure manually
# AZURE_SUBSCRIPTION_ID=
# AZURE_TENANT_ID=
# AZURE_CLIENT_ID=

# Notes:
# - Copy this file to .env and fill in your actual values
# - Never commit .env with real credentials to version control
# - For local dev, get a FREE tier Translator key from Azure Portal
# - Use Azure Key Vault for production secrets (automatically configured in Bicep)
# - Use Managed Identity in Azure instead of connection strings when possible

